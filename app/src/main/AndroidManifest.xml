<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.victor.paintboard">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:configChanges="locale"
            android:name="com.example.roundprogressbar.StartF"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main2Activity"
            android:configChanges="locale"/>
        <activity android:name=".MainActivity"
            android:configChanges="locale"/>
        <activity android:name="com.example.roundprogressbar.FindPc"
            android:configChanges="locale"/>
        <activity android:name="com.example.roundprogressbar.useStep"
            android:configChanges="locale"></activity>
    </application>

</manifest>
